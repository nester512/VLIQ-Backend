# Stub endpoints don't touch the DB; this URL is only here so Settings() loads.
# Replace with a real Postgres DSN when you wire up real handlers and want to run Alembic.
POSTGRES__POSTGRES_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/vliq
PORT=8000
ENV=local

# JWT
JWT_SECRET_SALT=dev-only-secret-please-replace-in-prod
