# Dev (Vite dev server proxy): leave unset — defaults to /api (relative path via Caddy/nginx)
# Dev (direct hit backend, no Docker): uncomment below
# VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_API_URL=http://localhost:8000/api/v1
