# DEPRECATED — что здесь лежало и куда делось

> **Источник правды по продукту: [`VLIQ PRD+BRD/Use cases VLIQ.md`](VLIQ%20PRD%2BBRD/Use%20cases%20VLIQ.md)** (+ [`FLOW Seller`s recipt.md`](VLIQ%20PRD%2BBRD/FLOW%20Seller%60s%20recipt.md)).
> Всё остальное из `docs/` удалено или заархивировано. Этот файл — «а тут было так»:
> сводка удалённых документов, чтобы не держать кучу отдельных файлов.
> Старые **закоммиченные** доки лежат в `docs/NOT_ACTUAL_DOCS/` (перенесены целиком, история в git).

---

## 0. Загрузка чека: deprecated endpoints / поведение (2026-06-22)

Переход на модель «одна отправка = один Receipt с 1–5 вложениями»
([design freeze](handover/MULTI-UPLOAD-DESIGN-FREEZE-2026-06-22.md)):

- **`POST /api/v1/receipts/qr-payload`** — **deprecated**. Самостоятельный QR-скан (без файлов) как
  способ загрузки убран (S3, В-2-A). Эндпоинт помечен `deprecated` в OpenAPI и возвращает
  `400 QR_ONLY_DEPRECATED` («Отсканированный QR можно приложить только вместе с фото или PDF»).
  Старые QR-only записи (`qr://inline`) по-прежнему читаются. Новый flow: `POST /receipts/upload`
  (multipart-пакет 1–5 файлов) с опциональным полем `scanned_qr`.
- **`POST /api/v1/receipts/finalize`** — контракт **изменён**: был single-file `{storage_uri, mime, brand_id}`,
  стал package `{upload_session, brand_id, idempotency_key, attachments[], scanned_qr?}` (после
  `POST /receipts/upload-urls`). Старый single-file `finalize` больше не поддерживается.
- **`POST /api/v1/receipts/upload-url`** (single) — **deprecated** (помечен в OpenAPI), оставлен для
  совместимости; новый пакетный аналог — `POST /receipts/upload-urls`.
- **Жёсткий 409 на дубль при ingest** (по `file_hash` / `qr_raw` / ФН-ФД-ФП) — **убран**. UNIQUE-индексы
  `uq_receipt_file_hash_active` / `uq_receipt_qr_raw_active` / `uq_receipt_fn_fd_fp` заменены на
  обычные (миграция `0005`). Дубль теперь — **сигнал** админу, а не отказ.
- Legacy-колонки `receipt.file_url / file_hash / file_kind` стали nullable (зеркало `attachments[0]`);
  будущая cleanup-миграция может их удалить, когда все читатели перейдут на `attachments`.

---

## 1. `SELLER_US_QUIZ_2026-06.md` (удалён)

Квиз бизнес-валидации: 23 вопроса по фактическому поведению системы, заполнен
стейкхолдерами 2026-06-04. **Все ответы перенесены** в «Use cases VLIQ.md»
(flow + журнал решений В-1…В-10), сам файл больше не нужен.

Сырые заметки стейкхолдеров из блока «Дополнительные пожелания» (оригинальная
формулировка, на случай споров о трактовке):

> Это и близко не Use Cases которые были заложены изначально.
>
> В главном интерфейсе админа должно показываться имя продавца.
> Статус needs_revision — конечное состояние, из которого сейчас нет выхода; админу действия недоступны.
> Продавец не видит фотографии чека — а должен.
> Флоу расчёта выплаты не понятен. Откуда начисляется кол-во денег за чек — тоже.
> Этого нет и не будет пока что, автоматического подсчёта нет — только Админ/Супер админ решают.
> Флоу: 1. Загрузка фоток 2. Отправка на проверку → Предпроверка наличия QR на загруженных фото.
> Пользователь видит не сумму, а кол-во одобренных/неодобренных чеков.
> Флоу со стороны админа: зашёл в «дейтинг» — видит все фото, может открыть фото чека и имя продавца.
> Админ должен понимать, что выплата уже сделана: к статусу «Одобрен» добавлять статус «Выплачен».

## 2. `SELLER_USER_STORIES_2026-06.md` (удалён)

23 «бизнес-US продавца», выведенные из реализации (7 эпиков, вердикты ✅/⚠️/🔴).
Стейкхолдеры оценили как «пародию на изначальные US» — реализация не является
источником требований. Полностью вытеснен «Use cases VLIQ.md».

## 3. `SELLER_FLOWS_2026-06.md` (удалён)

Карта **фактической реализации** seller-стороны на 2026-06-04 (14 разделов,
сверено с кодом). Устареет при приведении системы к Use cases; при расхождении
доверять коду. Самое ценное — сводка заглушек (пригодится для gap-анализа):

| # | Что | Состояние | Влияние |
|---|---|---|---|
| 1 | `OCR_MODE=demo` по умолчанию | заглушка | нет проверки ФНС; все чеки на ручную модерацию, бонус фикс **250 ₽** |
| 2 | `OFD_PROVIDER=fake` по умолчанию | заглушка | проверка в ФНС не выполняется; реальный `proverkacheka`-клиент готов, но выключен |
| 3 | «На доработку» (`revise`) → `rejected` | заглушка | продавец не может переотправить; флоу `needs_revision` не достроен |
| 4 | Авто-одобрение не шлёт уведомление | full-режим | в demo неактуально (одобряет админ) |
| 5 | Антифрод-флаг на выплате всегда «ок» | хардкод | чек-лист выплаты не отражает реальные риск-сигналы |
| 6 | Минимум выплаты 1 000 ₽ | только фронт | бэк не enforce-ит |
| 7 | `brand_id=1` при авто-создании | хардкод | все новые продавцы → бренд 1 |
| 8 | Прямой аплоад в S3 на тест-сервере | инфра | работает через fallback; разбор MinIO/CORS не сделан |
| 9 | Контакт поддержки `@vliq_support` | хардкод | один для всех брендов (по Use cases должен быть `@nester256`) |
| 10 | Luhn для карты выключен | намеренно | карта проверялась только по длине (реквизиты карты теперь вообще out of scope — S5) |

Также в реализации есть (а по Use cases быть не должно / иначе): самостоятельный
QR-скан как способ загрузки; жёсткое отклонение дублей (409) вместо предупреждения;
реквизиты выплаты в анкете регистрации и профиле; вывод только всего баланса целиком;
центр уведомлений внутри приложения; статус `needs_revision` в модели.

## 4. `SELLER_API_2026-05.md` (удалён)

«Бизнес use-case + бэклог контрактов» для scaffold-версии Seller API
(легенда ✅/🟡/🔴 по ручкам, Pydantic-схемы, этап «только контракты, тело 501»).
Привязан к устаревшим флоу (реквизиты в регистрации, минимум 1000 ₽ и т.п.) —
контрактный бэклог нужно перевыводить из «Use cases VLIQ.md» после gap-анализа.

## 5. `VLIQ-BOT-prototype.html` (→ `NOT_ACTUAL_DOCS/`)

HTML-прототип интерфейса бота (до Mini App). Перенесён в архив целиком.

## 6. `NOT_ACTUAL_DOCS/` (архив, перенесено владельцем репо)

`CONTRACT_AUDIT_2026-05.md`, `USERCASES_2026-05.md` (бывший «чек-лист что реально
работает» из CLAUDE.md), `USERSTORIES.md`, `Use cases.md`, `reviews/00–07`,
`ТЗ VLIQ BOT.docx` — историческая фактура, не источник требований.

---

*Создан 2026-06-04 при чистке `docs/`. Полные тексты удалённых незакоммиченных
файлов не сохранились (история git их не покрывает) — актуальное содержимое
перенесено в «Use cases VLIQ.md», остальное — выжимкой выше.*
